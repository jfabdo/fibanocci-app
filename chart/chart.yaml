---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "fibonacci-app"
  namespace: "default"
  labels:
    app: "{{ .Chart.metadata.name }}"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "{{ .Chart.metadata.name }}"
  template:
    metadata:
      labels:
        app: "{{ .Chart.metadata.name }}"
    spec:
      containers:
      - name: "{{ .Chart.metadata.name }}"
        image: "jfabdo/{{ .Chart.metadata.name }}:latest"
        env:
        - name: APP_VERSION
          value: "{{ .Chart.metadata.name }}"
        - name: USER
          value: "{{ .Chart.metadata.name }}"
---
apiVersion: "autoscaling/v2beta1"
kind: "HorizontalPodAutoscaler"
metadata:
  name: "fibonacci-app-hpa-dgrg"
  namespace: "default"
  labels:
    app: "fibonacci-app"
spec:
  scaleTargetRef:
    kind: "Deployment"
    name: "fibonacci-app"
    apiVersion: "apps/v1"
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: "Resource"
    resource:
      name: "cpu"
      targetAverageUtilization: 80
---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "fibonacci-app-service"
  namespace: "default"
  labels:
    app: "fibonacci-app"
spec:
  ports:
  - protocol: "TCP"
    port: 9001
    targetPort: 8080
  selector:
    app: "fibonacci-app"
  type: "NodePort"
---
