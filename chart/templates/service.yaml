---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "fibonacci-app-service"
  namespace: "default"
  labels:
    app: "{{ .Values.appname }}"
spec:
  ports:
  - protocol: "TCP"
    port: 80
    targetPort: 8080
  selector:
    app: "{{ .Values.appname }}"
  type: "NodePort"
